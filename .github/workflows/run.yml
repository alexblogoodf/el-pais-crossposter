name: Generate News Banner

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install system dependencies (for CairoSVG)
        run: |
          sudo apt-get update
          sudo apt-get install -y libcairo2-dev pkg-config libffi-dev

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate banner & prepare tweet
        run: python main.py generate
        env:
          PYTHONUNBUFFERED: '1'

      - name: Commit and Push banners
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "Update banner & queue [skip ci]" || echo "No changes to commit"
          git push

      - name: Post to Buffer (X/Twitter)
        run: python main.py post
        env:
          PYTHONUNBUFFERED: '1'
          BUFFER_API_KEY: ${{ secrets.BUFFER_API_KEY }}

      - name: Commit history update
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git commit -m "Update post history [skip ci]" || echo "No changes to commit"
          git push
